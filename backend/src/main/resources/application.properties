# Database Configuration (PlanetScale / Managed MySQL)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/ai_insurance}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Uday@2215302}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Server Configuration
server.port=${PORT:4000}
server.tomcat.max-http-form-post-size=10485760
server.tomcat.max-http-post-size=10485760
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# AI Microservice Configuration
ai.service.url=${AI_SERVICE_URL:http://localhost:8000}

# Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:giginsurance2026@gmail.com}
spring.mail.password=${MAIL_PASSWORD:ukygpuesokeqhdii}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.debug=false

# Razorpay Configuration
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_z1V4NXV4NXV4NX}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:rzp_test_secret_NXV4NXV4NXV4NX}

# Production CORS
app.frontend.url=${FRONTEND_URL:http://localhost:5173}

